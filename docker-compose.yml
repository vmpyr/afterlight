---
services:
  afterlight:
    build: .
    container_name: afterlight
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./afterlight-data:/data
    environment:
      - DB_PATH=/data/afterlight.db
      - ARTIFACTS_PATH=/data/artifacts
